# Copy to .env and fill in locally; .env is gitignored.
# This project uses Python config files / JSON+TOML for runs; add export lines here only if you wire them yourself.
# Example:
# export LLAMA_CPP_BINARY=/opt/llama.cpp/build/bin/llama-cli
# export LLAMA_CPP_MODEL=/models/model.gguf
